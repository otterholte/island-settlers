node_modules/
.DS_Store

# Development artefacts: screenshots from the headless capture rig, the
# reference art the build was judged against, and the live progress page.
# All of it is local working material, not part of the game.
progress/
server/data/
.playwright-cli/
output/

# The Android wrapper. It is a SEPARATE Capacitor project that happens to be
# unzipped next to the game — its own package.json, its own node_modules, its
# own Gradle build output, and a signing keystore that must never be committed
# anywhere. The game repo is not where it lives; see android-app/.
IslandSettlersApp/
android-app/
